/**
 * Static content / copy for the landing page.
 * Keep all marketing copy centralized here for easy updates
 * without touching component logic.
 *
 * Example structure:
 *   export const heroContent = { ... };
 *   export const featuresContent = [ ... ];
 *   export const faqContent = [ ... ];
 */

export {};
